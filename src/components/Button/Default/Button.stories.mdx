import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs/';

import Button from './Button';

<Meta
	title="UI/Button/Button"
	argTypes={{
		to: {
			control: false,
		},
		replace: {
			control: false,
		},
		className: {
			control: false,
		},
	}}
	component={Button}
/>

export const Template = ( args ) => {
	return <Button {...args} />
};

# Button

Если указан атрибут `to`, то компоент будет ссылкой `<Link>`, в противном случае `<button>`.

```js
import Button from 'components/Button/Default/Button';
```

<Canvas>
	<Story id="ui-button-button--primary"/>
	<Story id="ui-button-button--secondary"/>
</Canvas>


<ArgsTable of={Button} />

## Primary

<Canvas>
	<Story
		name="Primary"
		args={{
			value: 'Нажми на меня',
			priority: 'primary',
			disabled: false,
		}}
	>
		{Template.bind({})}
	</Story>
	<Story
		name="Primary - disabled"
		args={{
			value: 'Нажми на меня',
			priority: 'primary',
			disabled: true,
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>


## Secondary

<Canvas>
	<Story
		name="Secondary"
		args={{
			value: 'Нажми на меня',
			priority: 'secondary',
			disabled: false,
		}}
	>
		{Template.bind({})}
	</Story>
	<Story
		name="Secondary - disabled"
		args={{
			value: 'Нажми на меня',
			priority: 'secondary',
			disabled: true,
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>


## With text wrap

<Canvas>
	<Story
		name="With text wrap - primary"
		args={{
			value: 'Нажми\nна меня',
			priority: 'primary',
			disabled: false,
		}}
	>
		{Template.bind({})}
	</Story>
	<Story
		name="With text wrap - secondary"
		args={{
			value: 'Нажми\nна меня',
			priority: 'secondary',
			disabled: false,
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>